name: Judge

on: pull_request

jobs:
  judge:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: EtherOJ/EtherJudger@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
